<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.QuanLyChiTieu_Finly" parent="Theme.Material3.Light.NoActionBar">
        <!-- Màu chủ đạo -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryContainer">@color/purple_200</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Màu nền và chữ -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:navigationBarColor">@color/white</item>
        <item name="android:statusBarColor">@color/purple_500</item>

        <!-- App bar và các control -->
        <item name="android:colorBackground">@color/white</item>
        <item name="android:colorAccent">@color/purple_500</item>
    </style>

    <style name="ThemeOverlay.QuanLyChiTieu_Finly.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>
